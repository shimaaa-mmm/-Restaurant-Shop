export function getLocationSuggestions(address) {
  // Placeholder: در پروژه واقعی می‌توانید از سرویس مکان‌یابی استفاده کنید.
  // این تابع فقط یک لیست نمونه برمی‌گرداند.
  return [
    address,
    address + ', Tehran',
    address + ', Shiraz'
  ]
}
